[전체 레이아웃 구조 (Architecture)]
메인 화면(lv_screen_active())은 LV_LAYOUT_FLEX (Flexbox, Column 방향)를 사용하여 상단(고정), 중단(스크롤), 하단(고정) 3개의 영역으로 분할하여 구현하라.

1. 상단 영역: 현재 시각 표시 (Top Section)
컨테이너: 높이를 콘텐츠에 맞추고(LV_SIZE_CONTENT), 투명한 배경으로 설정.
현재 시각 레이블 (lv_label):
텍스트: "18:30" (테스트용 하드코딩, 추후 업데이트 가능하도록 변수 할당).
스타일: 화면 중앙 정렬, 가독성이 좋은 큰 폰트 적용.
시간 포맷(AM/PM/24H) 변경이 가능하도록 레이블 업데이트 함수를 따로 분리할 것.
구분선 (lv_line 또는 얇은 lv_obj):
시각 표시 아래에 화면 가로 너비(100%)를 꽉 채우는 얇은 가로 선(두께 1~2px, 검은색 계열)을 추가하여 영역을 구분.

2. 중단 영역: 과목 리스트 (Middle Section - Scrollable)
컨테이너 (lv_obj):
Flex 레이아웃에서 lv_obj_set_flex_grow(obj, 1)을 주어 상단과 하단을 제외한 나머지 공간을 모두 차지하도록 설정.
스크롤 설정: LV_OBJ_FLAG_SCROLLABLE 플래그를 활성화하고, 세로 스크롤만 가능하도록 설정 (LV_DIR_VER).
내부 아이템 정렬을 위해 Flexbox (Column 방향, 약간의 gap 추가) 적용.
리스트 아이템 (과목 카드):
각 과목은 하나의 컨테이너(lv_obj)로 만들고 Flexbox (Row 방향)로 내부를 구성.
너비는 부모의 100%, 높이는 콘텐츠에 맞춤.
터치 이벤트: LV_OBJ_FLAG_CLICKABLE 플래그를 추가.
선택 효과 (Selected State): 아이템을 터치했을 때 선택된 것을 시각적으로 알 수 있도록, 클릭 시 테두리(Border)가 굵어지거나 배경색이 옅은 노란색(ex: #F9F9DD)으로 변하도록 스타일(States)을 설정하라. (스케치의 3번째 아이템 참고)
리스트 아이템 내부 구성 요소:
좌측 아이콘: 작은 사각형 lv_obj (라운드 처리, 과목별로 초록, 빨강, 파랑 등 다른 배경색 적용).
우측 텍스트 영역 (Flex Column):
과목명 레이블 (lv_label): 예) "경영데이터분석" (중간 크기 폰트)
시간 레이블 (lv_label): 예) "19:00 ~ 20:00" (작은 크기 폰트)

3. 하단 영역: 액션 버튼 및 남은 시간 (Bottom Section)
컨테이너: 화면 하단에 고정. 높이는 콘텐츠에 맞춤(LV_SIZE_CONTENT), Flexbox (Column 방향, 중앙 정렬).
구분선: 상단과 마찬가지로 중단 스크롤 영역과 구분되는 얇은 가로 선을 하나 추가.
공부 시작 버튼 (lv_button):
크기: 너비를 화면의 약 80~90% 정도로 설정하여 터치하기 쉽게 큼직하게 만듦.
스타일: 모서리가 둥근 직사각형 (radius 값 높게), 배경색은 연한 파란색(Light Blue). 검은색 얇은 테두리(Border) 추가.
버튼 텍스트: "공부 시작" (lv_label 추가).
남은 시간 레이블 (lv_label):
위치: 버튼 바로 아래.
텍스트: "4시간 00분 00초 전..."
스타일: 스케치에 따라 폰트 색상을 **빨간색(Red)**으로 설정. 중앙 정렬, 작은 폰트 적용.

4. 동적 데이터 및 이벤트 처리 (Events & Timers)
시간 업데이트: lv_timer_create()를 사용하여 1초마다 실행되는 타이머를 만들어라. 이 타이머 콜백 함수 내에서 상단의 '현재 시각'과 하단의 '남은 시간' 레이블 텍스트를 업데이트하는 로직의 뼈대를 주석과 함께 작성해 둘 것.
선택 이벤트: 과목 리스트 아이템 클릭 시, 단일 선택(Single selection)만 되도록 기존 선택된 아이템의 스타일을 초기화하고 새롭게 클릭된 아이템에 선택 스타일을 적용하는 로직을 구현하라.