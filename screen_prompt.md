대기화면:
화면 기본 설정
방향: 세로 모드 (Portrait, 예: 240x320 또는 320x480 해상도 가정)
배경색: 흰색 (lv_color_white())
모든 텍스트는 기본적으로 검은색(lv_color_black())으로 설정해주세요.
한국어 텍스트가 포함되어 있으므로, 코드 내에 한글 폰트(예: lv_font_simsun_16 또는 사용자 정의 한글 폰트)를 적용하는 로직을 포함하거나 주석으로 명시해주세요.
[UI 요소 상세 설명 (위에서 아래 순서)]
1. 상단 타이틀 (Title Label)
타입: lv_label
텍스트 내용: "STUDY\nMANAGER" (개행 문자를 사용하여 2줄로 표시)
텍스트 정렬: 가운데 정렬 (LV_TEXT_ALIGN_CENTER)
위치 정렬: 화면 상단 중앙 (LV_ALIGN_TOP_MID)에 위치하며, 화면 맨 위에서 약간의 여백(Y축 offset, 예: 30~40px)을 줍니다.
스타일: 눈에 띄는 큰 영문 폰트를 적용해주세요.
2. 중앙 상태 박스 (Status Box)
타입: lv_obj (빈 컨테이너 객체) 위에 lv_label을 올린 형태
컨테이너(박스) 속성:
크기: 내부 텍스트를 감쌀 수 있는 적당한 직사각형 (예: width 200, height 60)
위치 정렬: 화면 정중앙 (LV_ALIGN_CENTER)
스타일: 배경색은 흰색(또는 투명), 테두리(Border)는 검은색 1~2px 두께로 설정하고 모서리 둥글기(Radius)는 0 또는 아주 작게(직각에 가깝게) 설정해주세요.
내부 라벨 속성:
텍스트 내용: "대기 중..."
위치 정렬: 컨테이너 객체를 기준으로 정중앙 (LV_ALIGN_CENTER)
3. 하단 안내 문구 (Instruction Label)
타입: lv_label
텍스트 내용: "스마트폰을\n상자위에 올려주세요" (개행 문자를 사용하여 2줄로 표시)
텍스트 정렬: 가운데 정렬 (LV_TEXT_ALIGN_CENTER)
위치 정렬: 화면 하단 중앙 (LV_ALIGN_BOTTOM_MID)에 위치하며, 화면 맨 아래에서 위로 약간의 여백(Y축 offset, 예: -40~-50px)을 줍니다.
스타일: 중앙 박스의 글씨보다는 약간 작지만 읽기 쉬운 한글 폰트를 적용해주세요.
[요구사항]
void create_study_manager_ui(void) 라는 이름의 함수 안에 모든 객체 생성 코드를 작성해주세요.
코드는 바로 컴파일이 가능하도록 논리적으로 작성되어야 하며, 각 UI 요소(상단, 중앙, 하단)별로 주석을 달아 가독성을 높여주세요.
불필요한 스크롤바는 보이지 않도록 처리해주세요 (lv_obj_clear_flag(obj, LV_OBJ_FLAG_SCROLLABLE)).