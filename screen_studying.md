[전체 레이아웃 구조 (Architecture)]
화면(lv_screen_active())은 LV_LAYOUT_FLEX (Flexbox, Column 방향)를 사용하여 상단(상태/진행률), 중단(타이머 카드), 하단(컨트롤 버튼) 3개의 영역으로 분할하라.
1. 상단 영역: 현재 시각 및 진행률 (Top Section)
컨테이너: 높이를 콘텐츠에 맞추고(LV_SIZE_CONTENT), 좌측 정렬(또는 약간의 패딩).
현재 시각 레이블 (lv_label):
텍스트: "19:45"
스타일: 좌측 상단 배치, 적절한 크기의 폰트, 이탤릭체 느낌이 가능하다면 적용.
프로그레스 바 (lv_bar):
사용자의 지시사항에 따라 반드시 lv_bar 위젯을 사용할 것.
크기: 너비는 부모 컨테이너의 100%(또는 좌우 여백을 둔 90%), 높이는 얇게(약 8~10px).
스타일: 배경은 투명하거나 연한 회색 테두리. 채워지는 인디케이터(LV_PART_INDICATOR) 부분의 색상은 **파란색(Blue)**으로 설정. (예: 30% 정도 채워진 상태로 초기화)
구분선: 바(Bar) 아래에 화면 전체 너비의 얇은 가로 구분선 추가.
2. 중단 영역: 메인 타이머 카드 (Middle Section)
컨테이너 (lv_obj):
lv_obj_set_flex_grow(obj, 1)을 사용하여 상/하단을 제외한 중앙 여백을 모두 채우도록 설정.
내부 정렬: Flexbox (Column 방향, 중앙 정렬 LV_FLEX_ALIGN_CENTER).
스타일: 화면 테두리에서 약간의 마진을 둔 큰 사각형 박스 형태. 검은색 얇은 테두리(Border) 적용.
타이머 카드 내부 구성 요소:
제목 레이블: "남은 시간" (일반 폰트 크기, 중앙 정렬)
타이머 레이블 (lv_label):
텍스트: "00:14:30"
스타일: 가장 큰 폰트 사용. 글자 색상은 파란색(Blue) (lv_obj_set_style_text_color 사용). 중앙 정렬.
현재 과목 정보 (Flex Row):
이전 메인 화면에서 선택했던 과목 정보를 보여주는 영역.
좌측 아이콘 (초록색 배경의 작은 라운드 사각형).
우측 텍스트 영역 (Column 방향): "경영데이터분석" (중간 폰트), "19:00 ~ 20:00" (작은 폰트).
3. 하단 영역: 컨트롤 버튼 (Bottom Section)
컨테이너: 화면 하단에 위치, LV_SIZE_CONTENT, Flexbox (Column 방향, 버튼 간 간격 gap 설정). 상단에 얇은 구분선 추가.
일시정지 버튼 (lv_button):
크기: 너비 약 80~90%, 터치하기 쉽게 높이 확보. 테두리 둥글게.
스타일: 배경색은 회색(Gray), 검은색 얇은 테두리.
텍스트: "▷|| 일시정지" (심볼 폰트 활용 권장).
조기종료 버튼 (lv_button):
크기: 일시정지 버튼과 동일.
스타일: 배경색은 연한 붉은색/분홍색 (Light Red/Pink), 검은색 얇은 테두리.
텍스트: "[-> 조기종료" (심볼 폰트 활용 권장).
4. 동적 데이터 및 이벤트 처리 (State & Event Logic)
타이머 로직 (lv_timer): 1초마다 '남은 시간' 레이블을 감소시키고, lv_bar의 값을 업데이트하는 타이머를 구현하라.
일시정지 버튼 토글 이벤트 (LV_EVENT_CLICKED):
버튼 클릭 시 상태를 체크하는 로직 구현.
정상 상태 -> 일시정지 클릭 시: lv_timer_pause()를 호출하여 시간을 멈춤. 버튼의 텍스트를 **"재개"**로 변경.
일시정지 상태 -> 재개 클릭 시: lv_timer_resume()을 호출하여 시간을 다시 흐르게 함. 버튼 텍스트를 다시 **"일시정지"**로 복구.
조기종료 버튼 이벤트 (LV_EVENT_CLICKED):
클릭 시 타이머를 삭제(lv_timer_delete)하고, 메인 화면으로 돌아가는(또는 팝업을 띄우는) 더미 콜백 함수를 연결해 둘 것.